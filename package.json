{
  "name": "innerverse",
  "version": "6.0.0",
  "private": true,
  "scripts": {
    "dev": "php -S localhost:8000",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
